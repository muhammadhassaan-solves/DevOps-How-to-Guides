1. ArgoCD Installation: 
https://argo-cd.readthedocs.io/en/stable/getting_started/
or using Helm Charts

2. To get initial admin password
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d ; echo

3. For getting access to arogcd ui
kubectl port-forward svc/argocd-server 8080:80 -n argocd 
or 
nohup kubectl port-forward svc/argocd-server 8080:80 -n argocd > argocd-pf.log 2>&1 &

Get Argo UI with load balancer(not recommended, if public access is required use tls and use ingress)
kubectl patch svc argocd-server -n argocd -p '{"spec": {"type": "LoadBalancer"}}'

4. Changing Default Sync Time
The default sync interval in ArgoCD controls how often ArgoCD automatically syncs your applications from Git. By default, this is 3 minutes (180 seconds).

a. Edit ArgoCD ConfigMap
kubectl -n argocd edit configmap argocd-cm

Add the following block at the end:
data:
  timeout.reconciliation: 10s #syncinterval

b. Restart Application Controller
kubectl -n argocd rollout restart statefulset argocd-application-controller

c. Verify Setting
kubectl -n argocd get configmap argocd-cm -o yaml | grep timeout.reconciliation
You should see:
timeout.reconciliation: 10s

ArgoCD now checks your Git repositories every 10 seconds for changes.

