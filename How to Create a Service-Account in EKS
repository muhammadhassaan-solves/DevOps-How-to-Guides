1. Configure IAM OIDC provider
export cluster_name=<CLUSTER-NAME>
oidc_id=$(aws eks describe-cluster --name $cluster_name --query "cluster.identity.oidc.issuer" --output text | cut -d '/' -f 5) 
aws iam list-open-id-connect-providers | grep $oidc_id | cut -d "/" -f4
eksctl utils associate-iam-oidc-provider --cluster $cluster_name --approve

2. Define the policy through ui (can be customer managed or aws managed)

3. Create a service account that will create the required role in iam and service account in eks
eksctl create iamserviceaccount \
  --name x-sa \
  --namespace app \
  --cluster <your-cluster-name> \
  --attach-policy-arn arn:aws:iam::<ACCOUNT_ID>:policy/sync-sa \  #customerdefined
  --attach-policy-arn arn:aws:iam::aws:policy/AmazonMSKFullAccess \  #awsdefined
  --approve \
  --override-existing-serviceaccounts

  4. For deleteing service account
eksctl delete iamserviceaccount \
  --name x-sa \
  --namespace app \
  --cluster <your-cluster-name>
